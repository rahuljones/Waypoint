import React from "react";

function CheckpointListElement({ checkpoint }) {
    return (
        <div className="CheckpointListItem">
            {checkpoint}
        </div>
    );
}

export default CheckpointListElement;